#include "main.h"

/**
 * positive_or_negative - check wheather the paramete
 * r are positive or negative
 *
 * @i: parameter to check
 *
 * Return: 0;
 */

void positive_or_negative(int i)
{
	if (i < 0)
		 printf("%d is negative\n", i);
	else
		printf("%d is positve\n", i);
}
